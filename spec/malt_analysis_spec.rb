require "spec_helper"

describe MaltAnalysis do
  it "has a version number" do
    expect(MaltAnalysis::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
